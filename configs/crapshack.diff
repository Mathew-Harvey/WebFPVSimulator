# diff
# Betaflight / SITL (SITL) 4.5.1
#
# CRAPSHACK. The stiff tune, cut for this simulator rather than ported to
# it, and named by the beta tester whose report it answers: "the default
# betaflight and karate tune is kinda floppy ... I had to push the pids all
# the way to like 200-300% to make it fly well." That report is a fair
# reading of the plant. This model's gyro has no noise in it, so the two
# real-quad tunes are paying for headroom against motor heat, frame
# resonance and D-term noise that does not exist here, and the price shows
# up as softness. On this plant, damping is free.
#
# NO RATES, same rule as every file here. Rates are the pilot's, chosen in
# Settings and appended to whichever tune is loaded; see configs/rates.js.
#
# PROVENANCE. The base is karate-race.diff, this repository's flattening of
# sugarK's race tune, whose filter tune (gyro lpf1 dynamic 500 to 1000 Hz,
# two dynamic notches, RPM filtering) is the phase lead that makes gains
# this high hold. On top of it, one slider block through the firmware's own
# config/simplified_tuning.c, exactly what dragging Betaflight
# Configurator's sliders does. Nothing here is a hand-typed PID.
#
# THE SLIDER VALUES WERE SWEPT, NOT GUESSED. Candidates at master 140, 165,
# 185 and 200 with D and feedforward variations were measured against the
# default and Karate with instant full steps, 25 percent small-signal
# steps, hard stick-release stops and the propwash proxy in
# scripts/flight-report.js. The numbers that chose this file, roll axis,
# default tune first, this tune second:
#
#   25% stick step   overshoot 18.3% -> 5.7%, rise90 29 -> 22 ms
#   instant full yaw rise90 172 -> 133 ms, overshoot 6.1% -> 2.3%
#   hard stop bounce 22 -> 16 deg/s
#   descent-and-catch wobble 19 -> 14 deg/s
#
# The small-signal row is the one that answers "floppy": mid-stick is where
# a race quad lives, and a fifth of overshoot on every correction is mush.
# Master 200 bought 1 more point of small-signal overshoot and gave back 2
# ms of full-stick rise; 185 is where the probes stop improving. D above
# 115 (a 130/160 candidate) slowed the stop from 65 to 70 ms and doubled
# the bounce, so 115 is the ceiling, not a compromise.
#
# The applied result, read back out of the module through sim_bf_get:
#   roll  P83 I96 D93 Dmin63 F277
#   pitch P91 I106 D108 Dmin72 F303
#   yaw   P83 I96 F277
# against the stock 45/80/40/30/120. That is the "200-300%" the tester
# asked for, delivered through the sliders that keep the ratios sane.

batch start

board_name SITL

# ---- airframe, read by the plant ----
set motor_kv = 1900
set motor_poles = 14
set vbat_max_cell_voltage = 420
set vbat_min_cell_voltage = 350
set vbat_warning_cell_voltage = 350
set battery_capacity = 1300
set small_angle = 180

# ---- presets/4.5/filters/defaults.txt ----
set gyro_hardware_lpf = NORMAL
set gyro_lpf1_type = PT1
set gyro_lpf1_static_hz = 250
set gyro_lpf2_type = PT1
set gyro_lpf2_static_hz = 500
set gyro_notch1_hz = 0
set gyro_notch1_cutoff = 0
set gyro_notch2_hz = 0
set gyro_notch2_cutoff = 0
set gyro_lpf1_dyn_min_hz = 250
set gyro_lpf1_dyn_max_hz = 500
set gyro_lpf1_dyn_expo = 5
set gyro_filter_debug_axis = ROLL
set dshot_bidir = OFF
set simplified_gyro_filter = ON
set simplified_gyro_filter_multiplier = 100

# ---- presets/4.5/tune/defaults.txt, master half ----
set yaw_spin_recovery = AUTO
set dshot_idle_value = 550
set mixer_type = LEGACY
set yaw_motors_reversed = OFF

# ---- karate-race.diff, gyro lowpass filters ----
set gyro_lpf1_static_hz = 0
set gyro_lpf1_dyn_min_hz = 500
set gyro_lpf1_dyn_max_hz = 1000
set simplified_gyro_filter = OFF

# ---- karate-race.diff, gyro dynamic notches ----
set dyn_notch_count = 2
set dyn_notch_q = 450
set dyn_notch_min_hz = 125
set dyn_notch_max_hz = 650

# ---- karate-race.diff, RPM filtering and DShot600 ----
set dshot_bidir = ON
set rpm_filter_fade_range_hz = 100
set motor_pwm_protocol = DSHOT600

# ---- profile 0 ----
profile 0

# presets/4.5/tune/defaults.txt, profile half
set pid_at_min_throttle = ON
set anti_gravity_gain = 80
set anti_gravity_cutoff_hz = 5
set anti_gravity_p_gain = 100
set acc_limit_yaw = 0
set acc_limit = 0
set iterm_rotation = OFF
set iterm_relax = RP
set iterm_relax_type = SETPOINT
set iterm_relax_cutoff = 15
set iterm_windup = 85
set iterm_limit = 400
set pidsum_limit = 500
set pidsum_limit_yaw = 400
set throttle_boost = 5
set throttle_boost_cutoff = 15
set p_roll = 45
set i_roll = 80
set d_roll = 40
set f_roll = 120
set p_pitch = 47
set i_pitch = 84
set d_pitch = 46
set f_pitch = 125
set p_yaw = 45
set i_yaw = 80
set d_yaw = 0
set f_yaw = 120
set d_min_roll = 30
set d_min_pitch = 34
set d_min_yaw = 0
set d_max_gain = 37
set d_max_advance = 20
set abs_control_gain = 0
set use_integrated_yaw = OFF
set thrust_linear = 0
set transient_throttle_limit = 0
set feedforward_transition = 0
set feedforward_max_rate_limit = 90
set dyn_idle_min_rpm = 0
set tpa_mode = D
set tpa_rate = 65
set tpa_breakpoint = 1350
set tpa_low_rate = 20
set tpa_low_breakpoint = 1050
set tpa_low_always = OFF
set ez_landing_threshold = 25
set ez_landing_limit = 5

# presets/4.5/filters/defaults.txt, profile half
set dterm_lpf1_dyn_min_hz = 75
set dterm_lpf1_dyn_max_hz = 150
set dterm_lpf1_dyn_expo = 5
set dterm_lpf1_type = PT1
set dterm_lpf1_static_hz = 75
set dterm_lpf2_type = PT1
set dterm_lpf2_static_hz = 150
set dterm_notch_hz = 0
set dterm_notch_cutoff = 0
set yaw_lowpass_hz = 100
set simplified_dterm_filter = ON
set simplified_dterm_filter_multiplier = 100

# ---- karate-race.diff, D term filtering ----
set dterm_lpf1_dyn_expo = 7

# ---- karate-race.diff, misc ----
set thrust_linear = 0
set throttle_boost = 2

# ---- karate-race.diff, I term and PID sum limits ----
#
# Both carried across unchanged, with karate-race.diff's own reasoning:
# iterm_relax_cutoff 15 is what keeps a wound I term from dumping as
# reverse rate when the stick centres on this plant, and pidsum_limit_yaw
# 1000 is what lets Betaflight's anti windup actually engage on a saturated
# yaw axis. Both matter MORE here, because I and P are higher.
set iterm_limit = 500
set pidsum_limit_yaw = 1000

# ---- the crapshack slider block ----
#
# RPY, not karate's RP, and the difference is the yaw column of every
# measurement above. The sliders leave yaw alone in RP mode, so karate's
# yaw is the stock 45/80/120 and it answers a full pedal in 131 ms. In RPY
# the master multiplier reaches yaw too, and the same step lands in 133 ms
# from a 172 ms default with a third of the overshoot. Yaw D stays zero,
# as Betaflight ships it.
#
# simplified_i_gain 65: I must NOT ride the master all the way up. I is
# multiplied by the master like everything else, so 65 at master 185 is
# still I 96 against the stock 80, and that is as far as it can go before
# the wind-and-dump bounce karate-race.diff documents at its own I block
# comes back. 75 was measured at 7 deg/s more stop bounce; 65 matches the
# best bounce of the sweep.
set simplified_pids_mode = RPY
set simplified_master_multiplier = 185
set simplified_pi_gain = 100
set simplified_i_gain = 65
set simplified_d_gain = 115
set simplified_dmax_gain = 140
set simplified_feedforward_gain = 125
set simplified_pitch_pi_gain = 105
set simplified_pitch_d_gain = 100
simplified_tuning apply

# ---- karate-race.diff, feedforward ----
set feedforward_max_rate_limit = 100

# ---- karate-race.diff, TPA ----
set tpa_rate = 70
set tpa_breakpoint = 1250

# ---- presets/4.3/rc_link/generic/250hz_race.txt, as karate-race.diff
# carries it: feedforward_boost 10 rather than the published 18, for the
# stick-release kick documented there. Boosts of 12 and 15 were both in
# the sweep and neither moved a single probe number this tune's way. ----
set feedforward_averaging = 2_POINT
set feedforward_smooth_factor = 35
set feedforward_jitter_factor = 4
set feedforward_boost = 10
set rc_smoothing_auto_factor = 25
set rc_smoothing_auto_factor_throttle = 25

batch end
